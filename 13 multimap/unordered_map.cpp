#include<bits/stdc++.h>
using namespace std;

void unorderedmap(){
    // unordered map have keys but it's not in sorted order instead everything stored in randomised
    // map works in log(n) time complexity but unordered map most of the cases runs in Biig O(1) in constant time
    // for wrost case in unordered map is Big O(N) time
}

int main(){
    return 0;
}