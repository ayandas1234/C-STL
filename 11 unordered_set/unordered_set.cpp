#include<bits/stdc++.h>
using namespace std;

void explain_unordered_set(){
    unordered_set<int> st;
    //lower bound and upper bound function doesn't work here
    //rest all of the function are same as set
    //it doesn't store elements in sorted order but it stores unnique elements
    //annd most of the function excuted in unordered_set have time constant tmme complexity mmeans Big O(1)
    //once in a blue moon in the worst case the time complesxity will be Big O(N)
    

}

int main(){

    return 0;
}